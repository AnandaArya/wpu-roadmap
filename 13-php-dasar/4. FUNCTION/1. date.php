<?php
    // Built in function punya php yang bisa dipakai gratis
    // Date
    // untuk menampilkan tanggal dengan format tertentu
    // echo date("l, d-M-Y");

    // Time
    //  UNIX-Timestamp / EPOCH time
    // detik yang sudah berlalu sejak 1 Januari 1970
    // wiki/Unix_time
    // echo time();
    // echo date("l", time()-3600*24*100);

    // mktime
    // membuat sendiri detik yang sudah berlalu
    // mktime(0,0,0,0,0,0);
    //  jam, menit, detik, bulan, tanggal, tahun
    // echo date("l",mktime(0,0,0,3,14,2003));

    // strtotime
    // echo date("l",strtotime( "14 march 2003"));

    // Function yang sering dipakai
    // Date/Time
    // time()
    // date()
    // mktime()
    // strtotime()

    // String
    // strlean() menghitung panjang sebuang strng
    // strcmp() menggambungkan 2 buah string
    // explode() memecah sebuah string menjadi array dipakai ketika kita ingin mengambil nama sebuah file
    // htmlspecialchars() untuk menjaga dari orang yang iseng ingin masuk ke web kita atau hacker

    // utility
    // var_dump() mencetak isi dari sebuah variabel, array, object
    // isset() apakah sebuah variabel pernah dibikin atau belum akan menghasilkan boolean
    // empty kebalikan dari isset ada isinya atau tidak
    // die() menghentikan program kita
    // sleep() memberhentikan sementara misal sleep 2 detik

    // User-defined Function = function yang kita

?>
